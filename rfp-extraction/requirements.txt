pymupdf==1.28.2
pytesseract==0.3.13
pdf2image==1.17.0

# Not imported by any Phase 1 code -- pulled in only to generate the
# OCR-fallback evidence fixture (evidence/ocr-fixture-page.png). Later
# phases will need it for real; kept unpinned here since Phase 1 has no
# actual dependency on a specific version.
pillow
